# Contributing to Engram
We welcome pull requests! Since this is a high-performance vector database, please ensure all new features merge cleanly with the `.leg` logophysical file system. Before submitting a PR, ensure you run `cargo clippy` and `cargo test`.
