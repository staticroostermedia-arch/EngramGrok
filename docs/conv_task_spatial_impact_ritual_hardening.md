# conv:task:spatial_impact_ritual_hardening

**Type**: conv:task
**Parent Arc**: `conv:arc:engram_self_verification_roadmap` (and meta skill evolution work)
**Status**: In Progress (First Execution Complete)
**Created**: 2026-05-26

## Goal

Harden the "Spatial-Manifold Change Discipline" (Pre-Edit Manifold Impact Recon + Post-Edit Delta Capture) into an operational, self-reinforcing ritual by:
- Identifying real gaps in how the built Tree-Sitter/AABB spatial ingestion participates in the non-flat relational and momentum layers.
- Executing the full discipline on a real code change inside the spatial tooling itself.
- Creating living manifold records (praxis advancement + explicit continuation blocks) so future agent instances inherit both the improved utility and the ritual pattern geometrically.

This directly serves the Inheritance Principle and Against Flat Knowledge by making code changes on the memory system itself into auditable, high-momentum events in the same substrate.

## Scope
- Review daemon + engram-ast + mcp spatial query paths with fresh expert grasp of ritual + geometric goals.
- Surface actionable gaps (especially lack of automatic relations from AST ingestion and thin `recall_in_file` output).
- Select and execute one small, high-signal edit using the full Pre-Edit → Edit → Post-Delta loop per the updated `engram-working-memory` skill and `praxis:spatial_manifold_impact_analysis`.
- Record everything geometrically (`remember`/`update` + multiple `relate` edges to ritual anchors and the new praxis).
- Create this `conv:task` checkpoint following the established project pattern.
- Advance the living praxis anchor with the outcome.

## Current State (Post First Execution)

- Full Pre-Edit recon performed on target area (`mcp.rs` `recall_in_file` handler) using:
  - `context_for_file`
  - `recall` + `query_with_momentum` on spatial/praxis concepts
  - `search_by_relation` + `visualize` on `praxis:spatial_manifold_impact_analysis`
- Selected gap: `recall_in_file` returned only bare concept names + line ranges, creating high friction for the Pre-Edit impact analysis the new skill demands.
- Real edit executed: Enriched `recall_in_file` output to include CRS + short provlog/signature snippet per result (small, safe, high-utility change in `crates/engram-server/src/mcp.rs`).
- Post-Delta capture complete:
  - Created `spatial_impact__recall_in_file_richer_output_2026` block.
  - Multiple relations wired (`advances`, `result_of`, `first_real_application`).
  - `praxis:spatial_manifold_impact_analysis` updated via correct `mcp_engram_update` (OP_ADD, thermodynamic history preserved, dv recorded).
- The new praxis now has its "first real application" recorded with live manifold links.

**This is the first documented end-to-end execution of the spatial impact ritual on the system that implements the spatial system.**

## Work Performed (This Session)

- Conducted systematic tool-driven review of spatial ingestion and query paths while grounded in the living ritual anchors.
- Identified 4 concrete gaps (primary: thin spatial query output + complete lack of automatic relational gluing from the daemon).
- Chose and executed the minimal high-leverage improvement to `recall_in_file` while strictly following the Pre-Edit/Post-Delta protocol defined in the working-memory skill.
- Recorded the delta using the geometric write hygiene rules (no forget+remember).
- Created strong relational connections between the improvement, the praxis, the meta skill evolution record, and the wake-up ritual anchor.
- Updated the praxis itself with the execution history.

## Session Notes

- The edit directly reduces the cost of following the new discipline, increasing the probability it will actually be used on future self-modifications.
- Performing the loop on the spatial tooling itself was the highest-signal first test possible.
- The fact that AST data for the current source tree lives in dedicated `_ast` stalks (when `ENGRAM_LINKED_WORKSPACE` is set) was re-confirmed during recon — correct behavior but means ritual must account for stalk switching in some environments.
- No blockers. The thermodynamic update on the praxis succeeded cleanly.

## Next Steps

1. Use the now-richer `recall_in_file` in future real development work on Engram (especially daemon, engram-ast, mcp handlers) to generate more momentum for the praxis.
2. Consider a follow-on small change that addresses Gap B (automatic lightweight relations from daemon after AST extraction — "defined_in_file", sibling relations) — this would be the next logical hardening of the ritual.
3. When creating future conv:task files or working on skills, explicitly run the spatial impact protocol on the relevant files.
4. Update AGENT_INTEGRATION_GUIDE.md or working_memory.md with a short "Spatial Impact Ritual in Practice" example once we have 2-3 real executions.
5. Eventually evaluate whether a dedicated `mcp_engram_spatial_impact_preview` convenience tool (wrapping the recon steps) would be valuable as a Praxis protocol.

## Success Criteria (for this task)

- [x] At least one real Pre-Edit → Edit → Post-Delta loop executed on Engram source using the new discipline.
- [x] Improvement recorded in the manifold with proper relations to ritual anchors and the target praxis.
- [x] This conv:task checkpoint created and linked.
- [x] The `praxis:spatial_manifold_impact_analysis` block advanced with thermodynamic update.
- [ ] At least one additional real-world use of the enriched spatial tooling during ordinary development (future session).

**Status Update**: First execution cycle complete. The ritual now has a living, auditable first instance. Momentum on the spatial impact praxis has begun.

---

**Related Blocks** (recorded in manifold):
- `praxis:spatial_manifold_impact_analysis`
- `meta:skill_evolution_2026`
- `spatial_impact__recall_in_file_richer_output_2026`
- `ritual:wake_up_anchor`

All work performed under the active spatial-manifold and ritual evolution discipline.